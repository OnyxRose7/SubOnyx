# SubOnyx
 Simple passive and active information gathering bash script for finding subdomains of poorly configured websites passively and actively by extracting them from "index.html" and then requesting the zone transfere file from every nameserver the domain uses 
